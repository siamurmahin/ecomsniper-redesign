# Build output and dependencies.
dist
node_modules
package-lock.json

# Generated — `npm run fetch:fonts` writes this, and reformatting it would
# only produce a diff the next run undoes.
src/styles/fonts.css

# Session notes are prose, wrapped by hand where the meaning wants a break.
SESSION-NOTES*.md
